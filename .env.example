# The only variable the browser app reads.
#
# It must include the /api/v1 prefix: main.ts calls
# app.setGlobalPrefix("api/v1"), so pointing this at the bare host answers 404
# on every request.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
